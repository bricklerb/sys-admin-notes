## Steps to configure startup.nsh linux
`FS0:` <br>
`edit startup.nsh`
add to the file
<ul>
      <li>`FS0:`<li\>
      <li>`cd EFI`<li\>
      <li>`cd Debian` (or whatever directory grub is in)<li\>
      <li>`grubx64.efi`<li\>
</ul>

`reset`
